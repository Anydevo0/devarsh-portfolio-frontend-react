# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env overrides — .env.example and .env.production are committed on purpose
# (VITE_API_BASE_URL isn't a secret; committing .env.production removes a
# no-CI-so-easy-to-forget footgun), but a bare local .env stays out of git.
.env

coverage

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
